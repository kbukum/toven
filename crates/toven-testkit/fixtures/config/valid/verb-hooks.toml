# Project-level per-verb lifecycle hooks.
[project]
name = "verb-hooks"

[ecosystems.rust]
manifests = ["Cargo.toml"]

[hooks.release]
pre = ["fmt-check", "lint"]
post = ["notify-release"]

[hooks.bump]
pre = ["validate"]

[hooks.coverage]
pre = ["build"]
