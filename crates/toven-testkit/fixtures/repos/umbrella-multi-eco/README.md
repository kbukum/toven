# `repos/umbrella-multi-eco/` — placeholder

Seed for the multi-ecosystem umbrella scenario (federation, steps 11/13). Grown incrementally by the step that needs it; this slice lands only the tree + the `single-rust` seed the `SampleRepo` builder materializes.
