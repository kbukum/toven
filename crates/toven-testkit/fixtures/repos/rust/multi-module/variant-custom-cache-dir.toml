# A `--config` variant that points the task cache at an in-repo directory instead
# of the platform cache root.
[project]
name = "multi-module-rust"

[toven.cache]
dir = ".toven/cache"

[ecosystems.rust]
manifests = ["Cargo.toml"]
run_strategy = "leaf-to-top"
