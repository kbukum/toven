# no-ecosystem

A repository with no detectable ecosystem manifest (no `Cargo.toml` or `go.mod`). `toven init` here produces a `[project]`-only config and must surface actionable guidance rather than a silent no-op.
