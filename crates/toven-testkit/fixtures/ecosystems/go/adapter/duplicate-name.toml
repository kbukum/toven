# Two Go modules whose repo-relative directories fold to the same identity:
# `svc/api` (path separator → `-`) and `svc-api` both resolve to the name
# `svc-api`, so discovery must abort with a duplicate-module conflict.
modules = ["svc/api/go.mod", "svc-api/go.mod"]
