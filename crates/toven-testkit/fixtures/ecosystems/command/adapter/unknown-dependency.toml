# A `depends_on` referencing a module that was never declared: discovery must
# reject this typo at the trust boundary rather than silently dropping the edge.
[[modules]]
name = "site"
root = "apps/site"
depends_on = ["ghost"]
