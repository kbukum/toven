fixture = "rust-workspace-cache"
assert = "none"

[[invocations]]
label = "first-arg"
command = "run"
task = "arg-stamp"
args = ["alpha"]

[[invocations]]
label = "second-arg"
command = "run"
task = "arg-stamp"
args = ["beta"]

[[invocations]]
label = "cached-second-arg"
command = "run"
task = "arg-stamp"
args = ["beta"]

