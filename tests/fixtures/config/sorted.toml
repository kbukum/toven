[profiles.zed]
adapter = "rust"

[profiles.zed.tasks]
z-last = { argv = ["cargo", "test"] }
a-first = { argv = ["cargo", "fmt"] }

[profiles.alpha]
adapter = "python"

[profiles.alpha.tasks]
test = { argv = ["python", "-m", "pytest"] }
