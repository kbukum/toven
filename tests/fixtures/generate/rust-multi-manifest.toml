# Generated by toven generate.
# Review before committing; existing config is never overwritten unless --overwrite is used.

[project]
schema = 1
name = "project"
root = "."

[profiles.main]
adapter = "rust"
execution = "spawn-each"
module_arg_template = ["-p", "{module.package}"]
resource_group = "cargo:{project.root}"

[profiles.main.discovery]
manifests = ["contrib/Cargo.toml", "core/Cargo.toml"]
