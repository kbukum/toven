# PLAN across the polyglot umbrella; the jsonl stream carries every ecosystem's
# unit under the pinned clock.
repo: polyglot/umbrella
steps:
  - id: 01-plan-build
    argv: [--output, jsonl, plan, build]
    stdout: { match: exact }
