# `driver list` reports each bundled adapter's link state — a query body on
# stdout (pipeable, like `modules`/`tasks`).
repo: polyglot/umbrella
steps:
  - id: 01-driver-list
    argv: [driver, list]
    stdout: { match: normalized }
